export type Point = {
    lat: number;
    lon: number;
    group: number;
}

export type Order = {
    "id": number,
    "lat": number,
    "lon": number,
    "address": string,
    "group": number
}